// このファイルが属するパッケージ名を宣言する。
package types

// この行で構造体の型定義を始める。
type GourmetSearchParams struct {
	// この行は処理の一部として必要な操作を実行する。
	Address string

	// この行は処理の一部として必要な操作を実行する。
	Genre string

	// この行は処理の一部として必要な操作を実行する。
	Keyword string

	// この行は処理の一部として必要な操作を実行する。
	Start int

	// この行は処理の一部として必要な操作を実行する。
	Count int

	// この行は処理の一部として必要な操作を実行する。
	Lat float64

	// この行は処理の一部として必要な操作を実行する。
	Lng float64

	// この行は処理の一部として必要な操作を実行する。
	Range int
	// ここで処理ブロックを終了する。
}

# テスト未実施ファイルの理由説明

このドキュメントでは、プロジェクト内でユニットテストが実施されていないファイルと、それぞれのテストが不要と判断される理由を説明します。

## 📋 目次

- [Frontend (TypeScript/React)](#frontend-typescriptreact)
  - [エントリーポイント](#エントリーポイント)
  - [型定義ファイル](#型定義ファイル)
  - [定数ファイル](#定数ファイル)
  - [エクスポート専用ファイル](#エクスポート専用ファイル)
  - [プレゼンテーション専用コンポーネント](#プレゼンテーション専用コンポーネント)
  - [API クライアント](#api-クライアント)
  - [シンプルなカスタムフック](#シンプルなカスタムフック)
- [Backend (Go)](#backend-go)
  - [エントリーポイント](#エントリーポイント-1)
  - [ミドルウェア](#ミドルウェア)
  - [型定義ファイル](#型定義ファイル-1)
- [テスト済みファイル一覧](#テスト済みファイル一覧)
- [まとめ](#まとめ)

---

## Frontend (TypeScript/React)

### エントリーポイント

#### `frontend/src/main.tsx`

**テスト不要の理由:**

1. **フレームワークの初期化コード**: React の DOM 初期化処理のみを行う最小限のコード
2. **外部依存性が高い**: `ReactDOM.createRoot`というフレームワーク API に依存
3. **テストの実益が低い**: このファイルのロジックは、React フレームワーク自体が保証
4. **E2E テストでカバー**: アプリケーション起動は統合テストや E2E テストで確認可能

**ファイルの役割**: アプリケーションのブートストラップ（起動処理）

---

#### `frontend/src/App.tsx`

**テスト不要の理由:**

1. **統合的な性質**: 複数のコンポーネントとフックを組み合わせるコンテナコンポーネント
2. **個別コンポーネントでテスト済み**:
   - `SearchForm.test.tsx` - 検索フォームの動作
   - `RestaurantList.test.tsx` - レストラン一覧の表示
   - `Pagination.test.tsx` - ページネーション機能
   - `useRestaurantSearch.test.ts` - 検索ロジック
3. **UI の結合部分**: 個々の部品の組み合わせであり、ロジックは含まれない
4. **E2E テストでカバー**: 実際のユーザーフローは統合テストで確認すべき

**ファイルの役割**: アプリケーション全体のレイアウトとコンポーネント統合

---

### 型定義ファイル

#### `frontend/src/types.ts`

**テスト不要の理由:**

1. **実行可能コードがない**: TypeScript の型定義（インターフェース）のみ
2. **コンパイル時チェック**: TypeScript コンパイラが型の整合性を保証
3. **他のテストで間接的にテスト**: この型を使用する全てのファイルでテストされる
4. **ランタイム動作がない**: 型定義はコンパイル後に削除され、実行時には存在しない

**ファイルの役割**: アプリケーション全体で使用する型定義の集約

---

#### `frontend/src/vite-env.d.ts`

**テスト不要の理由:**

1. **Vite 環境の型定義**: Vite フレームワークの型情報を提供する宣言ファイル
2. **自動生成ファイル**: Vite が管理する標準的な型定義
3. **実行可能コードなし**: 純粋な型宣言のみ

**ファイルの役割**: Vite 環境の型サポート

---

### 定数ファイル

#### `frontend/src/constants.ts`

**テスト不要の理由:**

1. **静的データのみ**: 定数とオブジェクトリテラルの定義のみ
2. **ロジックがない**: 計算や条件分岐などの処理を含まない
3. **使用側でテスト**: この定数を使用するコンポーネント（`SearchForm`, `Pagination`等）でテスト済み
4. **変更リスクが低い**: サービスエリアコードは仕様で固定されている

**ファイルの役割**: アプリケーション全体で使用する定数の管理

---

### エクスポート専用ファイル

#### `frontend/src/components/index.ts`

**テスト不要の理由:**

1. **再エクスポートのみ**: コンポーネントをインポートして再エクスポートするだけ
2. **ロジックがない**: 何の処理も行わない
3. **コンポーネント自体がテスト済み**: 各コンポーネントは個別にテスト済み
4. **バレルエクスポート**: インポート文を簡潔にするための慣習的パターン

**ファイルの役割**: コンポーネントの一括エクスポート（Barrel Export）

---

### プレゼンテーション専用コンポーネント

#### `frontend/src/components/ErrorMessage.tsx`

**テスト不要の理由:**

1. **シンプルなプレゼンテーション**: Props を受け取って表示するのみ
2. **ロジックがない**: 条件分岐や状態管理を含まない
3. **視覚的確認が主**: エラーメッセージの見た目はビジュアルリグレッションテストで確認すべき
4. **使用側でテスト**: このコンポーネントを使う側（`App.tsx`）で統合的にテスト可能

**ファイルの役割**: エラーメッセージの表示 UI

---

#### `frontend/src/components/LoadingSpinner.tsx`

**テスト不要の理由:**

1. **状態を持たない**: Props も受け取らない完全な静的コンポーネント
2. **ロジックがない**: HTML と CSS のみで構成
3. **視覚的要素**: ローディングアニメーションの見た目が重要
4. **汎用的**: どこでも使える単純な再利用可能コンポーネント

**ファイルの役割**: ローディング中のスピナー表示

---

#### `frontend/src/components/RestaurantDetail.tsx`

**テスト不要の理由:**

1. **シンプルな表示ロジック**: レストランデータを受け取って表示するだけ
2. **ロジックが最小限**: 条件分岐やデータ変換を含まない
3. **視覚的要素が主体**: モーダルのレイアウトとデザインが主要な機能
4. **統合テストでカバー**: ユーザーがレストラン詳細を開く操作は E2E テストで確認すべき

**ファイルの役割**: レストラン詳細モーダルの表示

---

### API クライアント

#### `frontend/src/api/client.ts`

**テスト不要の理由:**

1. **汎用的な薄いラッパー**: `fetch` API の薄いラッパー関数
2. **上位層でテスト済み**: この関数を使用する`restaurantApi`や`genreApi`でテスト済み
3. **環境依存**: ネットワーク通信のテストはモックが複雑になる
4. **標準 API に依存**: `fetch`はブラウザ標準 API で信頼性が高い

**ファイルの役割**: API 通信の基礎関数

---

#### `frontend/src/api/genreApi.ts`

**テスト不要の理由:**

1. **シンプルなラッパー**: `apiGet`を呼び出してキャッシュを管理するだけ
2. **依存コンポーネントでテスト済み**:
   - `useGenres.test.ts`でモックを通じてテスト済み
   - `storageService.test.ts`でキャッシュロジックをテスト済み
3. **統合テストで十分**: API とキャッシュの統合はフックレベルで確認済み
4. **類似パターン**: `restaurantApi`と同じパターンで、そちらはテスト済み

**ファイルの役割**: ジャンル情報の取得とキャッシュ管理

---

### シンプルなカスタムフック

#### `frontend/src/hooks/useModal.ts`

**テスト不要の理由:**

1. **シンプルな状態管理**: `useState`と`useCallback`の基本的な使用のみ
2. **React の標準パターン**: 開閉フラグとデータを管理する一般的なパターン
3. **使用側でテスト**: このフックを使う`App.tsx`で統合的にテスト可能
4. **ロジックが最小限**: 複雑な条件分岐やビジネスロジックを含まない
5. **汎用的**: どんな型のデータでも扱える再利用可能なユーティリティ

**ファイルの役割**: モーダルの開閉状態管理

---

## Backend (Go)

### エントリーポイント

#### `backend/main.go`

**テスト不要の理由:**

1. **アプリケーション起動コード**: サーバーの初期化と起動のみを行う
2. **依存注入のみ**: 各コンポーネントを初期化して結合するだけ
3. **個別コンポーネントでテスト済み**:
   - `handler_test.go` - ハンドラーの動作
   - `service_test.go` - サービスの動作
4. **統合テストでカバー**: サーバー全体の動作は統合テストや E2E テストで確認
5. **環境変数依存**: ポートや API キーなど環境に依存する部分が多い

**ファイルの役割**: アプリケーションのブートストラップとルーティング設定

---

### ミドルウェア

#### `backend/internal/middleware/cors.go`

**テスト不要の理由:**

1. **標準的な CORS 実装**: HTTP ヘッダーを設定するだけのシンプルな処理
2. **ロジックが最小限**: 条件分岐は OPTIONS メソッドのチェックのみ
3. **ハンドラーテストでカバー**: `handler_test.go`でミドルウェアを通したリクエストをテスト
4. **標準パターン**: Go 言語の HTTP ミドルウェアの典型的な実装
5. **設定値のみ**: ヘッダーの値は静的で、計算や変換を含まない

**ファイルの役割**: CORS (Cross-Origin Resource Sharing) ヘッダーの設定

---

### 型定義ファイル

#### `backend/internal/types/request.go`

**テスト不要の理由:**

1. **構造体定義のみ**: 実行可能なロジックを含まない
2. **コンパイル時チェック**: Go のコンパイラが型の整合性を保証
3. **使用側でテスト**: この型を使用する`handler_test.go`や`service_test.go`でテスト済み
4. **データ構造の定義**: リクエストパラメータの形式を定義するのみ

**ファイルの役割**: API リクエストパラメータの型定義

---

#### `backend/internal/types/response.go`

**テスト不要の理由:**

1. **構造体定義のみ**: 実行可能なロジックを含まない
2. **JSON マッピング**: JSON タグによる自動変換を Go の標準ライブラリが処理
3. **使用側でテスト**: `handler_test.go`や`service_test.go`でレスポンスの構造をテスト済み
4. **データ構造の定義**: API レスポンスの形式を定義するのみ

**ファイルの役割**: API レスポンスの型定義

---

## テスト済みファイル一覧

### Frontend

| ファイル                 | テストファイル                | テスト内容                  |
| ------------------------ | ----------------------------- | --------------------------- |
| `restaurantApi.ts`       | `restaurantApi.test.ts`       | レストラン検索 API 呼び出し |
| `storageService.ts`      | `storageService.test.ts`      | ローカルストレージ操作      |
| `useGenres.ts`           | `useGenres.test.ts`           | ジャンル取得フック          |
| `useRestaurantSearch.ts` | `useRestaurantSearch.test.ts` | レストラン検索フック        |
| `Pagination.tsx`         | `Pagination.test.tsx`         | ページネーション UI         |
| `RestaurantList.tsx`     | `RestaurantList.test.tsx`     | レストラン一覧表示          |
| `SearchForm.tsx`         | `SearchForm.test.tsx`         | 検索フォーム                |

### Backend

| ファイル       | テストファイル    | テスト内容                  |
| -------------- | ----------------- | --------------------------- |
| `genre.go`     | `handler_test.go` | ジャンル API ハンドラー     |
| `gourmet.go`   | `handler_test.go` | グルメ検索 API ハンドラー   |
| `hotpepper.go` | `service_test.go` | ホットペッパー API サービス |

---

## まとめ

### テスト戦略の方針

このプロジェクトでは、以下の方針に基づいてテストを実施しています：

1. **ロジックを持つファイルを優先**: ビジネスロジックや複雑な処理を含むファイルをテスト
2. **統合レベルでのテスト**: 個別の薄いラッパーより、それらを使う側でテスト
3. **型定義はコンパイラに委ねる**: TypeScript や Go の型システムを信頼
4. **視覚的要素は別の方法で**: UI コンポーネントの見た目はビジュアルリグレッションテストや E2E テストで確認

### テストカバレッジ

- **Frontend**: ビジネスロジック（API 通信、状態管理、フック）を重点的にテスト
- **Backend**: HTTP ハンドラーと API サービス層をテスト
- **未テスト部分**: エントリーポイント、型定義、シンプルな UI、設定ファイル

この戦略により、**テストのコストと効果のバランス**を最適化し、**保守性の高いテストスイート**を維持しています。

---

**作成日**: 2025 年 11 月 25 日  
**プロジェクト**: ホットペッパーグルメ検索アプリケーション
